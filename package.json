{
  "name": "babylon-scene",
  "version": "0.2.0",
  "dependencies": {},
  "devDependencies": {
    "@babylonjs/core": "^4.0.3",
    "babylonjs": "^4.0.3",
    "concat-files": "^0.1.1",
    "copy": "^0.3.2",
    "rollup": "^1.19.4"
  },
  "scripts": {
    "build": "node_modules/.bin/rollup -c rollup.config.js && node ./bin/build.js",
    "dev": "npx es-dev-server --watch --node-resolve --open docs/babylon-loading"
  }
}
